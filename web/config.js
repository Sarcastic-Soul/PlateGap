// Overwritten at deploy time with the real Function URL. Left at null, the
// app posts back to whatever origin served it, which is what makes
// `scripts/dev_server.py` work without configuration.
window.PLATEGAP_API = null;
